{% extends 'base.html.twig' %}

{% block title %}Carte de révision!{% endblock %}

{% block body %}

  <div class="card mt-3">
    <div class="card-header">
      {% include "partials/_nav.html.twig" %}
    </div>
    <div class="card-body">
      
      {% apply markdown_to_html %}
          {{ card | raw }}
      {% endapply %}

      <a href="{{ path('app_home') }}" class="btn btn-primary float-end">Suivante</a>
    </div>
  </div>

{% endblock %}